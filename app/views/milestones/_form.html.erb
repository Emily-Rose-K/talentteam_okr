<%= form_for([@okr, @project, @milestone]) do |form| %>

    <% if @milestone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@milestone.errors.count, "error") %> prohibited this project from being saved:</h2>
  
      <ul>
        <% @milestone.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
      <div class="field">
        <%= form.label :name %>
        <%= form.text_field :name %>
      </div>
    
      <div class="field">
        <%= form.label :due_date %>
        <%= form.text_field :due_date %>
      </div>
    
      <div class="actions">
        <%= form.submit %>
      </div>
  <% end %>